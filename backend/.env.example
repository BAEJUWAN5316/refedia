# Database Configuration
# 로컬 개발: SQLite
DATABASE_URL=sqlite:///./refedia.db

# Railway 프로덕션: PostgreSQL (Railway가 자동 설정)
# DATABASE_URL=postgresql://user:password@host:5432/database

# Redis Configuration
# 로컬 개발 (옵션, 없으면 캐싱 비활성화)
REDIS_URL=redis://localhost:6379

# Railway 프로덕션: Redis (Railway가 자동 설정)
# REDIS_URL=redis://host:6379

# JWT Authentication
SECRET_KEY=your-super-secret-key-change-this-in-production-use-at-least-32-characters
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# YouTube Frame Caching
FRAME_CACHE_EXPIRE_SECONDS=300
